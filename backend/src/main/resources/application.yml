server:
  address: ${SERVER_HOST}
  port: ${SERVER_PORT}

spring:
  application:
    name: dragons

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true

openai:
  api-key: ${OPENAI_API_KEY}